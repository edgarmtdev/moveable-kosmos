import styled from "./styles/navbar.module.css";
const Navbar = () => {
  return (
    <nav className={styled.navbar}>
      <h3 className={styled.title}>React Moveable</h3>
    </nav>
  );
};

export default Navbar;
